# Threat model checklist

The assertions [ADR-031](./adr/031-the-ai-harness-and-what-leaves-the-machine.md) is walked
against. Each one is checked against shipped code at the end of the 0.5 release, in this order.

`Shipped` means the code that satisfies the assertion is in the tree today and the evidence column
names it. A unit id means the assertion is satisfied by that unit of the 0.5 release, and the row
is not walkable until it merges.

| Assertion | Evidence | Status |
|---|---|---|
| No API key reaches `config.toml`, the database or disk. Keys are in the OS keychain, or in memory for the session where there is no native keychain. | `src-tauri/src/commands/ai.rs:97` (`mod keychain`), the no-keychain stub at `:132`, session fallback `AiState.keys:67`, invariant stated at `:5-8`; `crates/writ-core/src/config/` names no key field; the chat pane's keys live under their own keychain accounts (`writ_core::chat::Provider::key_account`) | Shipped |
| No note content, prompt text or response text reaches a log, a database row or an error string shown to the user. | `src-tauri/src/commands/ai.rs:21-22` for the rewrite path; `log_request` in `src-tauri/src/commands/chat.rs` logs host, provider, note count, byte count and turn count; `sanitize_update_error`, `src-tauri/src/commands/update.rs:293`, for the updater; `writ_core::activity::ActivityRecord` (`crates/writ-core/src/activity.rs:127`) has no content field, asserted over the serialised keys by `a_record_carries_no_key_that_could_hold_note_text` and again on a written line by `a_record_the_gate_writes_holds_no_note_text`; an MCP refusal names the note and a conflict copy relative to the notes folder (`tool_error`, `crates/writ-mcp/src/tools.rs`) and a chat refusal names the note and the copy's file name (`refusal`, `commands/chat.rs`) | Shipped |
| The MCP path makes no outbound request. Every read tool answers with the network denied. | Satisfied by `the_resolved_tree_carries_no_http_client`, `crates/writ-mcp/tests/no_tauri_dependency.rs`, which walks `cargo metadata` from `writ-mcp` over normal edges and fails on `tauri`, `reqwest`, `hyper`, `ureq` or `wry`; `rmcp` is taken with `default-features = false` so the HTTP transports resolve out. Still walked with the network-denied run | Shipped |
| Every write to a note file goes through the guard, and a refusal leaves the losing side on disk as a conflict copy. | `writ_core::notes::guard::decide_save` is called only inside `crates/writ-storage/src/guarded.rs` (`:254`, `:413`); `write_conflict_copy` at `guarded.rs:484`; the MCP tools and the chat pane reach notes only through the note host, pinned by `crates/writ-storage/tests/note_host_authority.rs` | Shipped |
| A client that has not been approved is refused, and its call performs no read and no write. | `writ_mcp::consent::ConfigGate` over `[mcp] approved_clients`, `crates/writ-mcp/src/consent.rs:117`, wired at `crates/writ-cli/src/main.rs:194`; the check is the first line of every tool method (`ToolHost::permit`, `crates/writ-mcp/src/tools.rs:620`), so a refusal opens no file; `DenyAll` is still the default a host takes | Shipped |
| No tool deletes or trashes a note. The registered write tools are `write_note`, `create_note` and `rename_note`. | `no_tool_deletes_trashes_or_moves_a_note`, `crates/writ-mcp/src/server.rs:394`, over the registered tool list; `an_enabled_server_refuses_a_write_tool` (`consent.rs`) holds `trash_note` in the refused set; `writ trash` (`crates/writ-cli/src/verbs.rs:70`) is the user's own CLI, not a client | Shipped |
| Consent is per client and split into read and write. Approving reading never grants writing. | `ClientApproval { name, first_seen, read, write }` in `[mcp] approved_clients`; the split is decided in `verdict`, `crates/writ-mcp/src/consent.rs:186`, asserted by `approving_reading_does_not_approve_writing` and its partner | Shipped |
| A call from an unknown client appends exactly one pending record, so the user can see it and decide. | `writ_storage::activity_log::append` with `Decision::Pending`, from `ConfigGate::decide`; asserted by `an_unknown_client_is_pending_and_leaves_one_record_to_decide_on` and `each_call_from_an_unknown_client_leaves_exactly_one_record` | Shipped |
| Consent is revocable in the app and by editing `config.toml`, and takes effect on the next call without a restart. | `ai.consented_hosts` (`crates/writ-core/src/config/ai.rs:56`) with `ai_consent_host` (`src-tauri/src/commands/ai.rs:407`) for hosts; `mcp_set_client_permission` and `mcp_forget_client` (`src-tauri/src/commands/activity.rs:211,224`) for clients, against the mtime-guarded `ConfigGate`; asserted by `an_approval_written_between_two_calls_lands_on_the_second`, `forgetting_a_client_returns_it_to_pending_on_the_next_call` and `an_unchanged_config_is_read_once_however_many_calls_arrive` | Shipped |
| A fresh configuration reaches no network and answers no client. `ai.enabled`, `mcp.enabled` and `ai.chat.enabled` are all false. | `default_enabled`, `crates/writ-core/src/config/ai.rs:13`, for `ai.enabled`; `default_enabled`, `crates/writ-core/src/config/mcp.rs:15`, for `mcp.enabled`; `default_chat_enabled`, `config/ai.rs:33`, for `ai.chat.enabled`; every field has a serde default (`config/mod.rs`) | Shipped |
| The server is a stdio process the client launches. Writ listens on no port. | `writ mcp`, `crates/writ-cli/src/mcp.rs`, served over stdio by `writ_mcp::server::serve_stdio` (`crates/writ-cli/src/main.rs:218`); `lsof -i` on the running server lists no socket | Shipped |
| The activity log is append-only and capped. It rotates at 5 MB and keeps one generation, and a malformed line is skipped. | `crates/writ-storage/src/activity_log.rs`, `O_APPEND` writes at `<data dir>/activity.jsonl`; rotation, the single generation, the skipped line and two appenders writing 500 records each are covered by `crates/writ-storage/tests/activity_log_tests.rs` | Shipped |

## Open gap

The third row is testable at the dependency level. What the test cannot see is a request made
through a crate it does not recognise, so the release checklist also drives the server over stdio
with the network denied and confirms every read tool answers: the test says no HTTP client is
reachable, the run says no request was made.
