<script is:inline>
  (function () {
    try {
      if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        document.documentElement.setAttribute('data-writ-intro', 'play');
      }
    } catch (_) {}
  })();
</script>
<div class="gut-col" style="padding:54px 12px 0 0"><span class="hero-gut">#</span></div>
<div style="padding:44px 0 22px 30px">
  <div class="hero-src" aria-hidden="true">
    <span class="hero-type"># A fast scratchpad that opens anything.</span><span class="hero-caret"></span>
  </div>
  <h1 class="hero-h1" style="font-family:var(--font-display); font-weight:800; font-variation-settings:'opsz' 72; font-size:clamp(34px,5vw,56px); letter-spacing:-.04em; line-height:1.02; color:var(--foreground); max-width:15ch">
    <span aria-hidden="true"><span class="hw">A</span> <span class="hw">fast</span> <span class="hw">scratchpad</span> <span class="hw">that</span> <span class="hw">opens</span> <span class="hw">anything<span style="color:var(--accent)">.</span></span></span>
    <span class="sr-only">A fast scratchpad that opens anything.</span>
  </h1>
  <p class="hero-sub" style="max-width:52ch; margin-top:18px; font-size:clamp(15.5px,1.6vw,18px); line-height:1.55; color:var(--muted)">Writ sits between a plain text editor and a full IDE. Open any file the instant you need it, scan code, fix a config, jot a note. Markdown, HTML, Mermaid and KaTeX render live, and every scratch stays searchable. Offline, keyboard-driven.</p>
  <div class="hero-ctas" style="display:flex; flex-wrap:wrap; gap:11px; margin-top:26px">
    <a href="#download" class="v2-btn-primary" style="display:inline-flex; align-items:center; gap:9px; height:48px; padding:0 24px; border-radius:11px; background:var(--accent); color:var(--accent-foreground); font-weight:600; font-size:15.5px; box-shadow:0 8px 24px -8px var(--glow)">Download Writ</a>
    <button type="button" data-cmd="demoRender" class="v2-btn-ghost" style="display:inline-flex; align-items:center; gap:8px; height:48px; padding:0 20px; border-radius:11px; cursor:pointer; background:transparent; color:var(--foreground); border:1px solid var(--border); font-weight:550; font-size:15.5px">
      <span style="font-family:var(--font-mono); font-size:12px; color:var(--accent)">▸</span> Try the window
    </button>
  </div>
</div>
